#pragma once

#include "Command.hpp"

// Minimal stub so CLI can include ShowCommand.hpp; real implementation
// lives in src/ShowCommand.cpp and full header can be expanded later.
class ShowCommand : public Command {};
