#include "IPNetwork.hpp"

// This translation unit exists to satisfy the build system which expects
// a source file for IP network related symbols. Implementations are
// provided in headers; this file remains intentionally empty.
