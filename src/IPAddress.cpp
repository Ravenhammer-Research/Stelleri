#include "IPAddress.hpp"

// This translation unit exists to satisfy the build system which expects
// a source file for IP address related symbols. All implementations are
// header-only polymorphic classes, so this file is intentionally empty.
